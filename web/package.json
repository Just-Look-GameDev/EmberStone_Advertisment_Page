{
  "name": "web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "prebuild": "node -e \"const {execSync} = require('child_process'); try { execSync('python ../create_favicon.py', {stdio: 'inherit', cwd: process.cwd()}); } catch(e) { console.log('Note: Skipping favicon generation. Install Python and Pillow to auto-generate favicon.'); }\"",
    "build": "vite build",
    "preview": "vite preview",
    "generate-favicon": "python ../create_favicon.py"
  },
  "dependencies": {
    "@portabletext/vue": "^1.0.14",
    "@sanity/client": "^6.15.0",
    "date-fns": "^3.6.0",
    "pinia": "^3.0.4",
    "vue": "^3.5.26",
    "vue-router": "^4.6.4"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.3",
    "vite": "^7.3.0",
    "vite-plugin-vue-devtools": "^8.0.5"
  }
}
